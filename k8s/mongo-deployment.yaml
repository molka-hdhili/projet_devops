apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo
  namespace: dispatchr-cms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo
  template:
    metadata:
      labels:
        app: mongo
    spec:
      containers:
      - name: mongo
        image: mongo:3.6
        ports:
        - containerPort: 27017
